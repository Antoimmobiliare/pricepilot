# PricePilot - Dependencies
# Install with: pip install -r requirements.txt

# Dashboard
streamlit>=1.32.0

# Data & Analytics
pandas>=2.0.0
numpy>=1.24.0

# Charts
plotly>=5.18.0

# Regression in scatter trendline (usata in analytics tab)
statsmodels>=0.14.0

# API REST (opzionale – solo se si usa pricepilot/api/server.py)
fastapi>=0.110.0
uvicorn>=0.29.0
pydantic>=2.0.0

# (Opzionale) Jupyter per analisi ad-hoc
# notebook>=7.0.0

# Supabase Auth + database sync
# Pin leggero: le versioni piu recenti tirano pyiceberg/storage build su Python 3.14 Windows.
supabase==2.10.0
